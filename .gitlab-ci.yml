pages:
  image: node
  before_script:
    - pwd
  script:
    - cd client
    - npm --version
    - date
    - time yarn install
    - date
    - time yarn run generate
    - date
    - du -sh public/
    - mv public ../public
    - ls -a
    - pwd
    - cd ..
    - ls -a
    - pwd
  artifacts:
    paths:
      - public

test:client:
   image: angular/ngcontainer
   script:
     - pwd
     - cd client
     - time yarn install 
     - time yarn generate 
     - xvfb-run -a yarn test
